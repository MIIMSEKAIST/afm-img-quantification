# YOLO dataset config for the two-class AFM detector.
# The labelled detection dataset is provided separately (not in this repo).
# `path` is the dataset root; train/val/test are relative to it.
path: datasets/afm_detection
train: train/images
val: valid/images
test: test/images

nc: 2
names: ['afm_map', 'colorbar']
